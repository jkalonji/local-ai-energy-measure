description: Read, fix and re-run a tiny Python project (read, edit and run tools)
needs_tools: true
timeout_s: 900
prompt: >
  The unit tests in test_calc.py fail because of a bug in calc.py. Fix calc.py so that
  `python -m unittest` passes. Do not modify test_calc.py.
files:
  calc.py: |
    def add(a, b):
        return a - b


    def double(x):
        return x * 2
  test_calc.py: |
    import unittest

    from calc import add, double


    class CalcTest(unittest.TestCase):
        def test_add(self):
            self.assertEqual(add(2, 3), 5)

        def test_double(self):
            self.assertEqual(double(4), 8)


    if __name__ == "__main__":
        unittest.main()
checks:
  - file_unchanged: test_calc.py
  - command: ["{python}", "-m", "unittest", "-q"]
